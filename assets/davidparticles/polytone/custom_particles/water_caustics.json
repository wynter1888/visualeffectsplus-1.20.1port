{
        "initializer":{
          "lifetime": "240",
          "alpha": "0",
          "size": "10",
          "hitbox_size": 20
        },
        "ticker": {
          "alpha": "min(max((20 - sqrt(DISTANCE_SQUARED)) / 20, 0), 1) * (0.2 + 0.015 * sin(0.2 * AGE)) * sin((pi / LIFETIME) * AGE)",
          "remove_condition": "sqrt(DISTANCE_SQUARED) > 20"
        },
        "liquid_affinity": "liquids",
        "has_physics": true,
        "rotation_mode": "look_up",
        "exclusion_radius": 6,
        "lit": true,
        "light_level": 5,
        "render_type": "additive_translucent",
        "offset": [0, 0, 0]
}